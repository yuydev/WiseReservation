server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wise_reservation?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT配置
jwt:
  secret: WiseReservation2026SecretKeyForJWTTokenGeneration
  expiration: 86400000

# 微信小程序配置
wechat:
  appid: your-app-id
  secret: your-app-secret

logging:
  level:
    com.wise.reservation: debug
